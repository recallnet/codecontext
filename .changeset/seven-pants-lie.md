---
"@recallnet/codecontext-claude-plugin": patch
---

Expand the Claude Code plugin package README so the npm page carries install,
hook, command, and workflow details without requiring a jump back to the repo
root README.
