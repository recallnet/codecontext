---
"@recallnet/codecontext-cli": minor
"@recallnet/codecontext-eslint-plugin": minor
"@recallnet/codecontext-parser": minor
---

Adopt `{@link ...}` as the canonical supporting-reference syntax for `@context` annotations.

Local references should now use `{@link file:...}` and remote references may use `{@link https://...}`. Parser, lint, spec, and README examples were updated to match.
